<html>
  <head>
    <title>CMS Example - Jay Framework</title>
    <script type="application/jay-data">
      data:
    </script>
    <link rel="stylesheet" href="../styles/cms-theme.css" />
    <style>
      /* Home page styles */
      .home-page {
        max-width: var(--container-max);
        margin: 0 auto;
        padding: var(--section-gap) var(--page-padding);
      }

      /* Hero section */
      .hero-section {
        text-align: center;
        padding: 80px 40px;
        margin-bottom: 64px;
        background: linear-gradient(135deg, var(--bg-card) 0%, var(--bg-elevated) 100%);
        border-radius: var(--radius-lg);
        border: 1px solid var(--border);
      }

      .hero-section h1 {
        font-size: 3.5rem;
        font-weight: 800;
        margin-bottom: 16px;
        background: linear-gradient(135deg, var(--text-primary) 0%, var(--accent) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-section p {
        font-size: 1.2rem;
        color: var(--text-secondary);
        max-width: 600px;
        margin: 0 auto 32px;
      }

      /* Status section */
      .status-section {
        margin-bottom: 64px;
      }

      .status-card {
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: var(--radius-lg);
        padding: 32px;
      }

      .status-card h2 {
        font-size: 1.5rem;
        margin-bottom: 16px;
        color: var(--text-primary);
      }

      .status-card p {
        color: var(--text-secondary);
        margin-bottom: 16px;
      }

      .status-card code {
        display: block;
        background: var(--bg-primary);
        border: 1px solid var(--border);
        border-radius: var(--radius-md);
        padding: 16px;
        font-family: var(--font-mono);
        font-size: 0.9rem;
        color: var(--accent);
        margin: 16px 0;
        overflow-x: auto;
      }

      .status-list {
        list-style: none;
        padding: 0;
        margin: 16px 0 0;
      }

      .status-list li {
        padding: 8px 0;
        color: var(--text-secondary);
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .status-list li::before {
        content: '→';
        color: var(--accent);
      }

      /* Instructions */
      .instructions {
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: var(--radius-lg);
        padding: 32px;
      }

      .instructions h3 {
        font-size: 1.25rem;
        margin-bottom: 16px;
        color: var(--text-primary);
      }

      .instructions ol {
        margin: 0;
        padding-left: 24px;
        color: var(--text-secondary);
      }

      .instructions li {
        margin-bottom: 12px;
      }

      .instructions strong {
        color: var(--text-primary);
      }
    </style>
  </head>
  <body>
    <div class="home-page">
      <!-- Site Header -->
      <header class="site-header">
        <a href="/" class="site-logo">CMS<span>.</span></a>
        <nav class="site-nav">
          <a href="/" class="active">Home</a>
        </nav>
      </header>

      <!-- Hero Section -->
      <section class="hero-section">
        <h1>Wix Data CMS Example</h1>
        <p>This example demonstrates the Jay Framework's Wix Data plugin for content management.</p>
      </section>

      <!-- Status Section -->
      <section class="status-section">
        <div class="status-card">
          <h2>🚀 Getting Started</h2>
          <p>
            The Wix Data plugin is installed and ready. When you run the dev server, 
            it will automatically discover your Wix Data collections and generate a default configuration.
          </p>
          
          <h3 style="margin-top: 24px; margin-bottom: 12px;">What happens on first run:</h3>
          <ul class="status-list">
            <li>Plugin connects to your Wix site using the API credentials</li>
            <li>Fetches all available CMS collections</li>
            <li>Generates a default configuration with collections and references</li>
            <li>No components are generated by default (you opt-in by editing the config)</li>
          </ul>
        </div>
      </section>

      <!-- Instructions -->
      <section class="instructions">
        <h3>Next Steps</h3>
        <ol>
          <li>
            <strong>Configure Wix credentials</strong> - Add your site ID and API key to the wix-server-client config
          </li>
          <li>
            <strong>Run the dev server</strong> - Execute <code style="display: inline; padding: 2px 8px; margin: 0;">npm run dev</code> to start
          </li>
          <li>
            <strong>Check the generated config</strong> - The plugin will log discovered collections
          </li>
          <li>
            <strong>Enable components</strong> - Edit the generated config to enable item pages, list pages, etc.
          </li>
          <li>
            <strong>Create pages</strong> - Add .jay-html pages that use the generated contracts
          </li>
        </ol>
      </section>
    </div>
  </body>
</html>
