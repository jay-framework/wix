{
  "name": "@jay-framework/wix-media",
  "version": "0.18.4",
  "type": "module",
  "description": "Wix Media Manager plugin for Jay Framework — media index generation and agent-kit references",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "files": [
    "dist",
    "plugin.yaml",
    "agent-kit"
  ],
  "exports": {
    ".": "./dist/index.js",
    "./client": "./dist/index.client.js",
    "./plugin.yaml": "./plugin.yaml"
  },
  "scripts": {
    "build": "npm run clean && npm run build:client && npm run build:server && npm run build:types && npm run validate",
    "validate": "jay-stack-cli validate-plugin",
    "build:client": "vite build",
    "build:server": "vite build --ssr",
    "build:types": "tsup lib/index.ts --dts-only --format esm",
    "build:check-types": "tsc",
    "clean": "rimraf dist",
    "confirm": "npm run clean && npm run build && npm run test",
    "test": ":"
  },
  "dependencies": {
    "@jay-framework/component": "^0.18.4",
    "@jay-framework/fullstack-component": "^0.18.4",
    "@jay-framework/stack-server-runtime": "^0.18.4",
    "@jay-framework/wix-server-client": "workspace:^",
    "@jay-framework/wix-utils": "workspace:^",
    "@wix/media": "^1.0.247",
    "@wix/sdk": "^1.21.5"
  },
  "devDependencies": {
    "@jay-framework/compiler-jay-stack": "^0.18.4",
    "@jay-framework/vite-plugin": "^0.18.4",
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.5",
    "tsup": "^8.5.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.11"
  }
}
