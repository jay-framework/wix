/** Maximum slots returned to the client per listSlots query. */
export const MAX_SLOTS_PER_QUERY = 200;

/** Page size when querying booking services from Wix. */
export const SERVICES_PAGE_SIZE = 100;
