name: getCategories
description: Get categories for a Wix Data collection. Returns category names, slugs, and item counts.

inputSchema:
  collectionId: string

outputSchema:
  categories:
    - _id: string
      slug: string
      title: string
      itemCount: number
